import React from "react";
import useBookList from "./useBookList";

const bookList = useBookList();
// console.log(bookList, '==bookList==')
const Book = () => {
  return <div className="content">1234</div>;
};

export default Book;
